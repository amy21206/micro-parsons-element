{
  "name": "regex_helper_component",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r bin && tsc && rollup -c && cp -r ./src/external ./bin/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.12.3",
    "rollup": "^2.35.1",
    "rollup-plugin-filesize": "^9.1.0",
    "rollup-plugin-terser": "^7.0.2",
    "typescript": "^4.1.3"
  }
}
